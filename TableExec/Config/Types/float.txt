@declare: float default: 0
^(\-|\+)?\d+(\.\d+)?$

@formatter: "TableCore.Plugin.RawDataFormatter"

@json-input:
{name} = (float){input}["{name}"];

@binary-input:
{name} = {input}.ReadSingle();

@binary-output:
{output}.Write({name});